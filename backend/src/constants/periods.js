const MS_PER_DAY = 1000 * 60 * 60 * 24;
const MAX_MS_PER_31_DAY = MS_PER_DAY * 31;
const MAX_MS_PER_366_DAY = MS_PER_DAY * 366;

module.exports = {
    MS_PER_DAY,
    MAX_MS_PER_31_DAY,
    MAX_MS_PER_366_DAY
};
